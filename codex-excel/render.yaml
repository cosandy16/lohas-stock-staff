services:
  - type: web
    name: lohas-stock-staff
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py"
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.5
